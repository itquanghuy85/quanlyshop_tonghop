import 'package:flutter/material.dart';

class FinanceV2View extends StatelessWidget {
  const FinanceV2View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tài chính V2')),
      body: const Center(
        child: Text('Finance V2 đang được cập nhật...'),
      ),
    );
  }
}
